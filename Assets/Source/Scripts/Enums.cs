namespace Game
{
    public enum GameState
    {
        LoadingStart,
        LoadingExit,
        GameStart,
        GameExit,
        MenuOpen,
        MenuClosed,
        LevelPartTransitionStart,
        LevelPartTransitionEnd,
        WinOpen,
        WinClose,
        LoseOpen,
        LoseClose,
    }
}